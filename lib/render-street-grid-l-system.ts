import logger = require('../util/logger/index');

function renderStreetGridLSystem(seq: string[]): GeoJSON.Feature[] {
    logger.warn({seq}, 'Rendering');

    return [];
}

export = renderStreetGridLSystem;
